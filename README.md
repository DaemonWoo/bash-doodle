# 🐚 Изучение Bash

Мой учебный репозиторий по командной оболочке Bash.  
Основан на книге **«Learning the bash Shell» (Cameron Newham)** и личной практике.

Цель: уверенно работать в терминале, писать надёжные скрипты и автоматизировать задачи.

---

## 📋 План изучения

### 🔰 Основы оболочки
- [x] Синтаксис команд, аргументы, опции
- [x] Навигация по файловой системе (`pwd`, `ls`, `cd`)
- [x] Работа с файлами и папками (`mkdir`, `touch`, `cp`, `mv`, `rm`, `rmdir`)
- [x] Просмотр содержимого (`cat`, `less`, `head`, `tail`, `file`)
- [x] Права доступа (`chmod`, `chown`, `umask`)
- [x] История команд и автодополнение
- [x] Псевдонимы (`alias`, `unalias`)
- [x] Получение справки (`man`, `help`, `info`, `whatis`)

### ⚙️ Переменные и окружение
- [x] Создание и использование переменных
- [x] Переменные окружения и `export`
- [x] Специальные переменные (`$?`, `$$`, `$_`, `$!`, `$-`)
- [x] Параметры командной строки (`$0`, `$1`, `$#`, `$@`, `$*`)
- [x] Подстановки: команд (`$(...)`) и арифметические (`$((...))`)
- [x] Фигурные скобки и работа со строками (`${var%pattern}` и т.д.)
- [x] Маски имён файлов (globbing: `*`, `?`, `[]`)
- [x] Экранирование и кавычки (`\`, `'`, `"`)

### 🔄 Ввод/вывод и конвейеры
- [x] Перенаправление вывода (`>`, `>>`)
- [x] Перенаправление ввода (`<`)
- [x] Перенаправление ошибок (`2>`, `&>`)
- [ ] Конвейеры (`|`) и фильтры (`grep`, `sort`, `wc`, `tr`, `cut`)
- [x] Подстановка процессов (`<( )`, `>( )`)

### 🧠 Условные операторы и проверки
- [x] Команда `test` и её псевдоним `[ ]`
- [x] Улучшенные проверки `[[ ]]`<!-- шаблоны A*, &&, || -->
- [x] Проверка файлов, строк, чисел
- [x] Логические операторы (`&&`, `||`, `!`)
- [x] Конструкция `if ... elif ... else ... fi`
- [x] Конструкция `case ... esac`

### 🔁 Циклы
- [x] Цикл `for ... in ... do ... done`
- [x] Цикл `while` и `until`
- [x] Управление циклами (`break`, `continue`)
- [x] Цикл `select` для меню

### 📦 Функции и скрипты
- [x] Создание и вызов функций
- [x] Локальные переменные (`local`)
- [x] Возврат значений (`return`, echo)
- [x] Аргументы функций
- [x] Структура исполняемого скрипта (`#!/bin/bash`)
- [x] Отладка скриптов (`bash -x`, `set -x`, `set -e`, `shellcheck`)

### 🧪 Продвинутые темы
<!--trap for cleanup, exiting, ctrl+c catch
tmpdir=$(mktemp -d)
trap 'rm -rf "$tmpdir"' EXIT
-->
- [x] Обработка сигналов (`trap`)
- [ ] Работа с массивами
- [x] Ассоциативные массивы <!-- only works on bash version 4+ -->
- [x] Here-документы и Here-строки
- [x] Чтение ввода (`read`, `select`)
- [x] Временные файлы (`mktemp`)
- [ ] Подстановки и расширения
- [ ] Оптимизация и идиомы Bash

### 🔧 Практические проекты
- [ ] Скрипт для пакетного переименования файлов
- [ ] Парсер логов с подсчётом ошибок
- [ ] Простое меню для управления системой
- [ ] Скрипт резервного копирования с архивированием
- [ ] Мониторинг свободного места на дисках
- [ ] Утилита-шаблонизатор файлов (замена переменных)
